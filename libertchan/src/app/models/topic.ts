export class Topic {
  title: string;
  text: string;
  imageLink: string;

  constructor(title: string, text: string, imageLink: string) {
    this.title = title;
    this.text = text;
    this.imageLink = imageLink;
  }
}
